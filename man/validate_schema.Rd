% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_schema.R
\name{validate_schema}
\alias{validate_schema}
\title{Validate the schema of a data frame}
\usage{
validate_schema(x, required, allow_extra = TRUE)
}
\arguments{
\item{x}{A data.frame or tibble.}

\item{required}{Named character vector of expected classes for required columns,
e.g. c(id = "integer", amount = "numeric", date = "Date").}

\item{allow_extra}{Logical; if FALSE, flags extra columns not listed in \code{required}.}
}
\value{
A tibble with columns: check, column, expected, actual, status.
Each row is an issue found (empty tibble if no issues).
}
\description{
Checks that required columns exist and have the expected base class.
}
\examples{
df <- tibble::tibble(id = 1:3, amount = as.numeric(1:3))
validate_schema(df, c(id = "integer", amount = "numeric", date = "Date"))
}
